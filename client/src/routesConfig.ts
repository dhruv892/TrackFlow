export const ROUTES = {
  home: "/",
  dashboard: "/dashboard",
  // issue: (id: string) => `/issues/${id}`,
  // newIssue: "/issues/new",
  login: "/login",
  register: "/register",
  profile: "/profile",
} as const;
